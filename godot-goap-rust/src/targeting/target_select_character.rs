use crate::ai::working_memory::{FactQuery, FactQueryCheck, WorkingMemoryFactTypeKey};
use crate::sensors::sensor_types::SensorArguments;


pub fn select_character(args: &mut SensorArguments) {
    // let fact_query = FactQuery::with_check(FactQueryCheck::FactType(WorkingMemoryFactTypeKey::Knowledge));
    // let Some(fact) = args.working_memory.find_fact_with_max_confidence(fact_query) else {return;};
}
