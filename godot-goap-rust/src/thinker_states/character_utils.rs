// various utils used to control character behavior


// pub fn rotate_towards(delta: f64, character: &mut Gd<CharacterBody3D>, target: Vector3) {
//     let look_at_transform = character.get_transform().looking_at(target, Vector3::UP, true);
//     let new_transform = character.get_transform().interpolate_with(look_at_transform, 4.2 * delta as f32);
//     character.set_transform(new_transform);
// }
